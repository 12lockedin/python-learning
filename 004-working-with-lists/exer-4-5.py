#exer-4-5 list 1 to 1 million
#use min() and max() to see is the list is actually that
#use sum() to see how quickly Python can add a million numbers

nums = list(range(1,1000001))

print(min(nums))
print(max(nums))
print(sum(nums))